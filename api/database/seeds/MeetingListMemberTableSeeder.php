<?php

use Illuminate\Database\Seeder;
use Illuminate\Support\Facades\DB;

class MeetingListMemberTableSeeder extends Seeder
{
    /**
     * Run the database seeds.
     *
     * @return void
     */
    public function run()
    {
//        $lists = App\MeetingList::all();
//
//        App\User::all()->each(function ($user) use ($lists) {
//            $user->meetingLists()->attach(
//                $lists->random(rand(1, 3))->pluck('id')->toArray()
//            );
//        });
    }
}
